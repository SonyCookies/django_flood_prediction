After cloning, create a new virtual environment:
python -m venv venv

then activate it: 
venv\Scripts\activate

Once the virtual environment is active, install all the dependencies from the requirements.txt file:
pip install -r requirements.txt


